# 任务4结果分析：DBSCAN 密度聚类

最终 DBSCAN 参数为 `eps=0.5, min_samples=5`，生成 2 个簇、34 个噪声点，轮廓系数为 0.6559，ARI 为 0.4421。

DBSCAN 的轮廓系数较高，说明被归入簇的样本之间距离较紧密；但 ARI 不高，并且噪声点数量较多，说明其聚类结果与鸢尾花真实三分类标签的一致性有限。

从参数对比可以看出，eps 较小时噪声点数量大幅增加，eps 较大时簇容易合并。DBSCAN 不需要预先指定簇数量，适合发现任意形状簇和噪声点，但在鸢尾花这种类别边界较规则且有重叠的小数据集上，效果不一定优于 K-Means。
